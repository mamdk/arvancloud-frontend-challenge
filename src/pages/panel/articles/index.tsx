import styles from './index.module.sass';
import Section from 'src/components/ui/section';

function ArticlesPage() {
	return (
		<main className={styles.articlesPage}>
			<Section title={'All Posts'}>s</Section>
		</main>
	);
}

export default ArticlesPage;
